version: "2"

sql:
  - engine: "mysql"
    schema:
      - "sql/schema/*.sql"
    queries:
      - "sql/queries/*.sql"
    gen:
      go:
        package: "db"
        out: "internal/models/db"
        sql_package: "database/sql"
        emit_empty_slices: true


